# Plan 61 — Span Marker Redesign (roadmap seat)

## 1. Status

**TABLED (user, 2026-09-05)** — parked with plan 60 behind gate G60-RULINGS, sequenced at the
end of the roadmap after the task-list cleanup and the editor-functionality push (low-hanging
fruit → keybinds + minimum required editing → bend authoring). This file is the ROADMAP SEAT
for a design that already exists in full: the source of truth is
`docs/plans/todo/span-marker-redesign.md` (the founding principle, the location-only marker
record, the template model with the 2026-09-05 evolution section, the resolution and
invalidation laws, the settlement rider, the extent invariant, the signed span boundary
gestures, and the execution phases), together with `docs/plans/todo/chord-dictionary.md`
(the grip library the markers reference). Re-verify both against the code before execution —
they are todo-tier documents and may lag.

## 2. Goal

Markers as forced statement boundaries; grip templates `{name, string→finger, stops}` in the
song-scoped dictionary, referenced from markers; the template editor; the span boundary
gestures (signed 2026-09-05); and — as the closing beat — the let-ring hand coupling that
clips derived spans at plan 60's FHP shifts.

## 3. Sequencing (the three-beat ruling, user 2026-09-05)

Plan 60 Phases 1–2 land first (derivation must stand alone); this plan's marker/template
authoring executes together with plan 60 Phase 3 (FHP markers and span markers are ONE shared
object, built once); the hand coupling closes the sequence, gated on the census seam-vs-shift
agreement measure. An interim STOPGAP (session task list) may precede everything: materialize
current stored FHPs + current derived spans into hand-editable markers, ripped out when this
plan lands.

## 4. Final acceptance

Per the execution phases in the todo document, plus: the sighting gates recorded there (the
derived-default re-confirmation 60-Q5; the sub-4 width sighting from plan 60 rides the same
template arc), and the coupling readiness gate handed over from plan 60.
